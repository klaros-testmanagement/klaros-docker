Source of the original Apache Derby files: https://github.com/akaer/Dockerfiles/tree/master/klaros/ApacheDerby

To view all differences of a file, download the original file and compare it with "diff".

+ .start.sh to create the missing directories (Docker issues a message about which files are missing)
	CA_DIR_DATA=/data
	CA_DIR_BASE=$CA_DIR_DATA/catalina-base
	CA_DIR_TEMP=$CA_DIR_BASE/temp
	CA_DIR_LOGS=$CA_DIR_BASE/logs
	CA_DIR_CONF=$CA_DIR_BASE/conf
	CA_DIR_WEB=$CA_DIR_BASE/webapps
	KT_DIR_WEB=/root/klaros-testmanagement/webapps
	KT_DIR_CONF=/root/klaros-testmanagement/conf
	
+ .start.sh: Add "trap" so that Catalina can shutdown correctly.
+ Modify the auto-install.xml file
